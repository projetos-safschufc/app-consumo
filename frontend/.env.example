# URL base da API backend (usado no build de produção; em dev o proxy /api é usado)
# Produção com IP/porta variáveis: ex. http://10.28.0.124:5001/api (mesmo servidor, API na porta 5001)
# Se frontend e API estiverem no mesmo host (proxy reverso): use /api para URL relativa
VITE_API_BASE=http://localhost:5001/api

# Proxy em dev (opcional): deve coincidir com APP_PORT do backend. Padrão 5000; se backend usa 5001:
# VITE_PROXY_TARGET=http://localhost:5001
