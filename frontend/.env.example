# URL base da API backend (build/produção)
VITE_API_BASE=http://localhost:5001/api

# Proxy em dev (opcional): deve coincidir com APP_PORT do backend. Padrão 5000; se backend usa 5001:
# VITE_PROXY_TARGET=http://localhost:5001
